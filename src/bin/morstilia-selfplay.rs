//! Deterministic engine-vs-engine match driver (Stages 5 + 8).
//!
//! Identical to `selfplay`; both are thin wrappers around
//! [`morstilia::matchplay::cli`].

fn main() {
    morstilia::matchplay::cli::main();
}
